import SwiftUI

struct ResultPageView: View {
    @State var takenImage: UIImage
    @StateObject var mainViewModel: MainViewModel
    
    var body: some View {
        NavigationView {
            
        }
    }
}
